package link.stuf.exceptions.munch.ids;

import link.stuf.exceptions.munch.Id;

import java.util.UUID;

public class FaultId extends Id {

    public FaultId(UUID hash) {
        super(hash);
    }
}
